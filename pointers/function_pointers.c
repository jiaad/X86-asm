#include <stdio.h>
int (*f)(int *);
// f is pointer
// (*f) is pointer to function that has a single int* as arg as indicated by
// (*f) (int*) the paranthesis are required
//  without () int *f(int*) ==== (int*) f(int *) ==== function prototype
//
//

  (*tpf[])();
   return 0;
}

